# Jarvis 0.9.0 Beta — release verification

Published on September 8, 2026 at 15:43:11 UTC (12:43:11 in São Paulo).

## Downloads

- [Release and notes](https://github.com/paulovnas/jarvis/releases/tag/v0.9.0-beta)
- [Windows x64 installer](https://github.com/paulovnas/jarvis/releases/download/v0.9.0-beta/Jarvis_0.9.0-beta_x86_64-pc-windows-msvc-setup.exe)
- [macOS Apple Silicon installer](https://github.com/paulovnas/jarvis/releases/download/v0.9.0-beta/Jarvis_0.9.0-beta_aarch64-apple-darwin.dmg)

The release is public, marked as a prerelease, and contains all eight required assets. Unauthenticated requests returned HTTP 200 for the release and both installers.

## Source and delivery

- Completed application changes were committed and pushed first: `ff24549`.
- Desktop release configuration and version alignment: `9e9c595`.
- Native compatibility and CI assertion corrections: `1549538`, `925625c`.
- Immutable tag: `v0.9.0-beta`, commit `925625c47bfeb7acd5dc6eb9b21189d2dd0cf1ad`.
- [Successful validation of both platforms](https://github.com/paulovnas/jarvis/actions/runs/34240425424).
- [Successful official publication](https://github.com/paulovnas/jarvis/actions/runs/34244746471), including both build jobs and the publication job.
- Follow-up `57bcc35` stabilizes asynchronous provider-menu assertions on `main`; it changes tests only and preserves the release tag.

The workflow retains its compatible filename `release-macos.yml`, is displayed as **Release Desktop**, and publishes macOS Apple Silicon plus Windows x64 together. The existing updater key and protected signing environment are preserved. See [RELEASING.md](../RELEASING.md) for subsequent releases.

## Verification

| Platform | Frontend tests | Rust tests | Other gates |
| --- | --- | --- | --- |
| Windows x64 | 423 passed, 3 skipped | 405 passed, 16 ignored | Lint, typecheck, production build, all-targets Clippy and signed NSIS packaging passed |
| macOS Apple Silicon | 425 passed, 1 skipped | 404 passed, 15 ignored | Lint, typecheck, production build, all-targets Clippy, app/DMG signing and signer verification passed |

All eight public assets were downloaded again. Their sizes and SHA-256 hashes match GitHub's release metadata. The macOS archive and Windows installer signatures were independently verified against the existing public key embedded in Jarvis.

`latest.json` contains exactly `darwin-aarch64` and `windows-x86_64`. Each platform manifest contains only its matching platform, with the same version, date, notes, package URL and signature. All updater URLs point to the corresponding assets under the official `v0.9.0-beta` tag.

| Package | Bytes | SHA-256 |
| --- | ---: | --- |
| Windows NSIS installer | 14,985,377 | `fe206fcdfea3df528b68fe98c0023bcddb6c489024319de4bde1830c4a693174` |
| macOS DMG | 20,964,146 | `631c4a1b8e5c8f162fa8b475f78123e690d4b0e6fd60a5f35718cce098b6e913` |
| macOS updater archive | 20,632,501 | `967ee3c5da4fd33f54c8077234e33c2d4f137aa8704ed7e9cfdc5a2d0578c578` |

Local copies are in `release-artifacts/published-0.9.0-beta/` and remain outside Git. The follow-up test correction also passed ordered lint, typecheck, the complete frontend suite (423 passed, 3 skipped with one worker under local machine load), and production build.

## Installation limits

The Windows updater signature is valid, but the installer has no Authenticode publisher certificate (`NotSigned`). Windows may show an unknown-publisher or SmartScreen notice. macOS app/DMG signing is verified; Apple notarization remains unconfigured.

This release was downloaded and verified without installing it on this machine. An installed A-to-B upgrade and the remaining manual macOS feature checks are separate from the completed build/publication checks. The previously tracked localized MSVC informational linker message (`jarvis-9au`) appeared during local Rust tests; the Clippy gates passed.

Beads: release delivery `jarvis-y73`; asynchronous menu test correction `jarvis-y73.1`.
